# Files that should be managed by chezmoi but NOT committed to git
# Add these files to .gitignore as well

# Claude Code instructions (repo-only, not for home directory)
CLAUDE.md

# Private environment variables
.zshrc_private
.bashrc_private

# Google Cloud SDK - ignore logs, cache, and temporary files
.config/gcloud/logs
.config/gcloud/cache
.config/gcloud/.last_survey_prompt.yaml
.config/gcloud/.last_update_check.json
.config/gcloud/.metricsUUID
.config/gcloud/config_sentinel
.config/gcloud/default_configs.db

# Linux-only systemd overrides
{{ if ne .chezmoi.os "linux" }}
.config/systemd/user/plasma-powerdevil.service.d/restart.conf
{{ end }}
